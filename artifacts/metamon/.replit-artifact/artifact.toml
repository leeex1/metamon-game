kind = "web"
previewPath = "/"
title = "Metamon: Survival Rogue"
version = "1.0.0"
id = "artifacts/metamon"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20350

[services.development]
run = "pnpm --filter @workspace/metamon run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/metamon", "run", "build" ]
publicDir = "artifacts/metamon/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20350"
BASE_PATH = "/"
